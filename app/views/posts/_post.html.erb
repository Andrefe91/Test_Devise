<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

  <% if user_signed_in? %>
    <p>
      <strong>User:</strong>
      <%= User.find(post.user_id).email %>
    </p>
  <% end %>

</div>
